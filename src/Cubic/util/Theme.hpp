#pragma once

#include <dpp/dpp.h>

namespace cubic {
    namespace theme {
        namespace colors {
            constexpr auto primary = dpp::colors::cyan;
            constexpr auto secondary = dpp::colors::white;
            constexpr auto tertiary = dpp::colors::black;
        };
    };
};